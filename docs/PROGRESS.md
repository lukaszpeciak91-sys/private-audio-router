# Progress

- Added a dedicated Privacy Policy entry to the existing Settings modal and a single-layer Privacy Policy child page that reuses the established graphite sheet surface, border, typography, safe-area bounds, backdrop dismissal, and Android Back handling. Opening the policy replaces the Settings-root content within the same Dialog rather than stacking another modal; Back returns to the Settings root, while outside tap dismisses to the unchanged main screen. The exact approved English claim set is resource-backed. The panel is implemented with a `LazyColumn` so its body can scroll within the bounded sheet, but no runtime scroll interaction has been executed or claimed. Focused source and Compose UI contracts cover row presence, opening, title, outside dismissal, Back-to-Settings behavior, absence of modal stacking, and the authoritative English privacy claims. The first propagation pass added both keys to all 86 locale files but incorrectly copied English rather than semantically localizing them; this remains a localization **HOLD** and key parity is not presented as completion. Routing, diagnostics logic/reporting, overlay, proximity, permissions, manifest, network behavior, analytics, and Activity structure are unchanged.

- Upgraded the localization policy and repo-local workflow with one proportional high-risk factual-claim guardrail for privacy, security, permissions, accounts, diagnostics, backup, data handling, and similar copy. It requires bounded English source claims, semantic decomposition that preserves claim scope and strength, complete claim-set consistency, a mandatory reverse semantic gloss before PASS, and conservative HOLD treatment when exact factual scope lacks sufficient linguistic evidence.

- Implemented the focused `ASSISTANT` protected-session linger experiment: the unchanged 1.5-second playback-loss confirmation now begins one generation-safe 15-second retained-context window, exact assistant/speech resumption reuses the same track/mode/earpiece cycle without another routing attempt, and expiry or safety/lifecycle overrides use existing ordered cleanup. Communication/browser classifiers and end behavior, Fake Phone, UI/locales, service state projection, and proximity mechanics are unchanged. Automated and physical validation remain to be recorded.

- Disabled Android application-data backup at the sole application-level manifest declaration. This privacy hardening changes no routing, protected POC-5, diagnostics, settings, About, localization, overlay, proximity, permissions, services, network, or dependency behavior.

- Replaced the Settings diagnostic-report shortcut with a dedicated, release-facing Diagnostics screen using the existing OLED visual language. The screen projects Android/package environment metadata, observed communication-device capabilities and route, proximity support, overlay permission, authoritative controller/state evidence, trigger origin, and the last completed routing cycle into a small immutable user-facing summary. A completed cycle is successful only when Android reported the built-in earpiece and the cycle has no protected failure; accepted requests without earpiece evidence fail. Concise last-error categories are derived only for failed cycles while the unchanged technical reason remains in the existing saved report. The `ACTION_CREATE_DOCUMENT` report workflow now appears only at the bottom of Diagnostics, and Back returns directly to the main screen rather than nesting another modal. The older unreferenced POC-5 `DiagnosticScreen.kt` remains isolated and unchanged. New English/default `settings_diagnostics` and `diagnostics_*` source strings intentionally use English fallback pending a separate reviewed localization pass; existing frozen locale resources were not mechanically translated. Focused JVM projection tests cover empty, accepted-without-evidence, success, blocked/failure reason, all trigger origins, and route mappings. No routing detection, protected POC-5 sequencing, communication-device/mode call, cleanup ordering, proximity behavior, overlay routing, permission, capture, network, or analytics behavior changed.

- Added Galician / galego (`gl`) as one neutral, complete LTR stable-product locale in generic `values-gl` resources using Latin (`Latn`) script, intended to serve `gl`, `gl-ES`, and `gl-Latn-ES` with default identity `gl-Latn-ES`; no regional, script-specific, or duplicate resource tree was added. The frozen 28-string stable localization completed linguistic review at **PASS HIGH**, with 0 FIX, 0 POLISH, and 0 HOLD, and is preserved exactly. The temporary experimental `settings_fake_phone_pre_arm` key remains intentionally excluded. Frozen semantics distinguish ON `Activado` from runtime ACTIVE `Activo`, preserve the full `Preparado` / `En espera` / `Activo` / `Erro` paradigm, and protect reviewed `IA`, `Mini`, `Predeterminado`, `Configuración avanzada`, `saída de audio`, `auricular integrado do teléfono`, `informe de diagnóstico`, and accessibility/control wording. Persistent static/source contracts protect stable-key and placeholder parity, qualifier identity and duplicate-tree absence, exact high-risk literals, the temporary-key exclusion, and absence of a Galician compact override. Android instrumentation covers generated LocaleConfig discovery, actual Android resource resolution and LTR configuration for all three intended tags, formatted accessibility text, and production-equivalent shared 16f/15f/14f common-size measurement of all four natural Mini labels. Static/source-contract and JVM/Gradle results are recorded by this change's executed checks; Android instrumentation, its selected Mini size, emulator/runtime rendering and locale switching, accessibility behavior, and physical-device presentation remain unverified unless separately executed. No routing, audio, proximity, controller, diagnostic-core, Fake Phone, English/default, existing locale, shared Mini sizing/geometry, or unrelated production behavior changed.

- Added Lao (`lo`) as one neutral, complete LTR stable-product locale in generic `values-lo` resources using Lao (`Laoo`) script, intended to serve `lo`, `lo-LA`, and `lo-Laoo-LA` with default identity `lo-Laoo-LA`; no regional, script-specific, duplicate BCP-47, or unrelated variant tree was added. The frozen 28-string stable product localization completed FULL linguistic review plus targeted MINI-review at **PASS HIGH**, with 0 FIX, 0 POLISH, and 0 HOLD, and is preserved exactly. The reviewed audio-output wording has MEDIUM-HIGH replacement confidence; this is not a HOLD and did not authorize a rewrite. The temporary experimental `settings_fake_phone_pre_arm` key is intentionally excluded and falls back to default English; this is not a missing localization or Lao HOLD. Frozen semantics distinguish ON `ເປີດ` from runtime ACTIVE `ກຳລັງໃຊ້ງານ`, preserve the full `ພ້ອມ` / `ກຳລັງລໍຖ້າ` / `ກຳລັງໃຊ້ງານ` / `ຂໍ້ຜິດພາດ` paradigm, and protect reviewed `AI`, `ມິນິ`, `ການຕັ້ງຄ່າ`, `ຄ່າເລີ່ມຕົ້ນ`, `ຂັ້ນສູງ`, audio-output switching, diagnostic-report, built-in earpiece, Lao-script and combining-mark, mixed Lao/Latin, and accessibility-action wording. Persistent static/source contracts protect exact stable-key and placeholder parity, qualifier identity, duplicate-tree absence, high-risk literals, native-script presence, the temporary-key exclusion, and absence of a Lao compact override. Android instrumentation covers generated LocaleConfig discovery, actual Android resource resolution and LTR configuration for all three intended tags, localized Settings and accessibility retrieval, and production-equivalent shared 16f/15f/14f common-size Mini measurement. Static/source-contract and JVM/Gradle results are recorded by this change's executed checks; Android instrumentation, instrumentation-selected Mini size, emulator/runtime locale switching and rendering, system-font Lao glyph coverage, combining/tone-mark positioning, line height and clipping, accessibility behavior, and physical-device validation remain unverified unless separately executed. No routing, audio, proximity, controller, diagnostic-core, Fake Phone, English/default, existing frozen-locale, shared Mini, or unrelated production behavior changed.

- Added Georgian (`ka`) as one neutral, complete LTR stable-product locale in generic `values-ka` resources using Georgian (`Geor`) script, intended to serve `ka`, `ka-GE`, and `ka-Geor-GE` with default identity `ka-Geor-GE`; no regional, script-specific, or duplicate resource tree was added. The frozen 28-string stable product localization completed FULL linguistic review plus targeted MINI-review at **PASS HIGH**, with 0 FIX, 0 POLISH, and 0 HOLD, and is preserved exactly. The temporary experimental `settings_fake_phone_pre_arm` key is intentionally excluded and falls back to default English; this is not a missing localization or Georgian HOLD. Frozen semantics distinguish ON `ჩართულია` from runtime ACTIVE `აქტიურია`, preserve the full `მზადაა` / `მოლოდინშია` / `აქტიურია` / `შეცდომა` paradigm, and protect reviewed `AI`, `მინი`, `პარამეტრები`, `ნაგულისხმევი`, `დამატებითი`, audio-output switching, diagnostic-report, built-in earpiece, Georgian-script, and accessibility-action wording. Persistent static/source contracts protect exact stable-key and placeholder parity, qualifier identity, duplicate-tree absence, high-risk literals, the temporary-key exclusion, and absence of a Georgian compact override. Android instrumentation covers generated LocaleConfig discovery, actual Android resource resolution and LTR configuration for all three intended tags, accessibility resource retrieval, and production-equivalent shared 16f/15f/14f common-size Mini measurement. Static/source-contract and JVM/Gradle results are recorded by this change's executed checks; Android instrumentation, instrumentation-selected Mini size, emulator/runtime rendering and locale switching, Georgian glyph presentation, accessibility behavior, and physical-device validation remain unverified unless separately executed. No routing, audio, proximity, controller, diagnostic-core, English/default, existing frozen-locale, shared Mini, or unrelated production behavior changed.

- Added Mongolian (`mn`) as one neutral, complete LTR stable-product locale in generic `values-mn` resources using Cyrillic (`Cyrl`) script, intended to serve `mn`, `mn-MN`, and `mn-Cyrl-MN` with default identity `mn-Cyrl-MN`; traditional Mongolian script and `mn-CN`, Latin-script Mongolian, regional, script-specific, and duplicate resource trees are not part of this locale. The frozen 28-string stable product localization completed FULL linguistic review plus targeted MINI-review at **PASS HIGH**, with 0 FIX, 0 POLISH, and 0 HOLD, and is preserved exactly. The temporary experimental `settings_fake_phone_pre_arm` key is intentionally excluded and falls back to default English; this is a deliberate temporary-product-scope exclusion, not a missing translation or Mongolian HOLD. Frozen semantics distinguish ON `Асаалттай` from runtime ACTIVE `Идэвхтэй`, preserve the full `Бэлэн` / `Хүлээж байна` / `Идэвхтэй` / `Алдаа` paradigm, and protect reviewed `ХОУ`, `Мини`, `Өгөгдмөл`, `Нарийвчилсан`, natural audio-output switching, diagnostic-report, built-in earpiece, and accessibility wording. Persistent static/source contracts protect exact stable-key and placeholder parity, qualifier identity, unsafe duplicate/script-tree absence, Cyrillic orthography, high-risk literals, the temporary-key exclusion, and absence of a Mongolian compact override. Android instrumentation covers generated LocaleConfig discovery, actual Android resource resolution and LTR configuration for all three intended tags, accessibility resource resolution through the frozen controller description, and production-equivalent shared 16f/15f/14f common-size Mini measurement. Static/source-contract and JVM/Gradle results are recorded by this change's executed checks; Android instrumentation, instrumentation-selected Mini size, emulator/runtime rendering and locale switching, Cyrillic glyph presentation, accessibility behavior, and physical-device validation remain unverified unless separately executed. Routing, audio, proximity, controller architecture, diagnostic-core behavior, English/default copy, existing locale copy, shared Mini geometry/measured-fit logic, and shared direction/layout architecture are unchanged.

- Added Kazakh (`kk`) as one neutral, complete LTR stable-product locale in generic `values-kk` resources using Cyrillic (`Cyrl`) script, intended to serve `kk`, `kk-KZ`, and `kk-Cyrl-KZ` with default identity `kk-Cyrl-KZ`; no Latin-script, regional, script-specific, or duplicate qualifier tree was added. The frozen 28-string stable product contract completed FULL linguistic review plus targeted MINI-review at **PASS HIGH**, with 0 FIX, 0 POLISH, and 0 HOLD, and is preserved exactly. The temporary experimental `settings_fake_phone_pre_arm` key is intentionally excluded from Kazakh and falls back to default English; this deliberate product-scope exclusion is not an untranslated Kazakh HOLD. Frozen semantics distinguish ON `Қосулы` from runtime ACTIVE `Белсенді`, preserve the full `Дайын` / `Күтуде` / `Белсенді` / `Қате` Mini paradigm, and protect reviewed `ЖИ`, `Мини`, `Әдепкі`, `Қосымша`, audio-output switching, diagnostic-report, built-in earpiece, and accessibility wording. Persistent source contracts protect the qualifier identity, duplicate/Latin-tree absence, Cyrillic orthography, high-risk exact literals, temporary-key exclusion, and absence of a Kazakh compact override. Android instrumentation covers generated LocaleConfig discovery, actual localized-resource resolution and LTR configuration for all three intended tags, and production-equivalent shared 16f/15f/14f common-size Mini measurement. Static/source-contract and JVM/Gradle results are recorded by this change's executed checks; Android instrumentation, instrumentation-selected Mini size, emulator/runtime rendering, glyph presentation, accessibility behavior, and physical-device validation remain unverified unless separately executed. Routing, audio, proximity, controller architecture, diagnostic-core behavior, English/default copy, existing locale copy, shared Mini geometry/measured-fit logic, and shared direction/layout architecture are unchanged.

- Completed focused post-merge localization cleanup after the Assamese and Khmer merge: repaired the combined Android resource-resolution and Mini-measurement test blocks, restored independent Assamese and Khmer semantic-contract tests, and consolidated their generated LocaleConfig expectation without changing either frozen resource file. A repository-wide conflict-marker scan found no remaining markers; resource validation confirmed XML validity, exact key/placeholder parity, unique keys and locale trees, and no Assamese or Khmer Mini override.

- Added Khmer (`km`) as one neutral, complete LTR product locale in generic `values-km` resources using Khmer (`Khmr`) script, intended to serve `km`, `km-KH`, and `km-Khmr-KH` with default identity `km-Khmr-KH` and without regional, script-specific, legacy-alias, or duplicate trees. The unchanged 28-key English source contract completed FULL linguistic review plus targeted MINI-review at PASS — HIGH with 0 FIX, 0 POLISH, and 0 HOLD; the supplied frozen resource set is preserved exactly. Frozen semantics distinguish ON `បានបើក` from runtime ACTIVE `សកម្ម`, preserve the complete `ត្រៀមរួចរាល់` / `កំពុងរង់ចាំ` / `សកម្ម` / `កំហុស` paradigm, and retain reviewed `AI`, collision-reviewed `Mini`, `លំនាំដើម`, `កម្រិតខ្ពស់`, audio-output switching wording, diagnostic-report terminology, the built-in call-earpiece description, and reviewed parallel accessibility controls. Persistent contracts protect qualifier identity and duplicate-tree absence, native Khmer script, high-risk exact literals, key/placeholder parity, and absence of a Khmer compact-state override. Android instrumentation covers generated LocaleConfig discovery, actual localized-resource resolution and LTR configuration for all three intended tags, and production-equivalent shared 16f/15f/14f common-size Mini measurement. Static source/resource checks passed. Gradle/JVM tests were not executed because the Gradle wrapper download was blocked by the environment proxy. Android instrumentation, instrumentation-selected Mini size, emulator/runtime Khmer shaping and presentation, and physical-device presentation remain unverified. Routing, diagnostic core, proximity behavior, controller architecture, audio architecture, English/default and existing locale copy, shared Mini geometry/measured-fit architecture, and shared direction/layout architecture are unchanged.
- Added Assamese (`as`) as one neutral, complete LTR product locale in generic `values-as` resources using Bengali / Eastern Nagari (`Beng`) script, intended to serve `as`, `as-IN`, and `as-Beng-IN` with default identity `as-Beng-IN`; no regional, script-specific, legacy-alias, or duplicate tree was added. The unchanged 28-key English source contract completed FULL linguistic review plus targeted MINI-review at PASS — HIGH with 0 FIX, 0 POLISH, and 0 HOLD; the supplied frozen resource set is preserved exactly. Frozen semantics distinguish ON `অন আছে` from runtime ACTIVE `সক্ৰিয়`, preserve the complete `সাজু` / `অপেক্ষাৰত` / `সক্ৰিয়` / `ত্ৰুটি` paradigm, and retain reviewed `AI`, collision-reviewed `মিনি`, `ডিফ’ল্ট`, `উচ্চখাপৰ`, audio-output wording, diagnostic-report terminology `ডায়েগন’ষ্টিক ৰিপ’ৰ্ট`, and built-in call-earpiece term `বিল্ট-ইন ইয়েৰপিচ`. Persistent contracts protect qualifier identity and duplicate-tree absence, native locale identity, high-risk exact literals, accessibility action parallelism, key/placeholder parity, and absence of an Assamese compact-state override. Android instrumentation covers generated LocaleConfig discovery, actual localized-resource resolution and LTR configuration for all three intended tags, and production-equivalent shared 16f/15f/14f common-size Mini measurement; no locale-specific override or production geometry/font-size change was added. Static/source-contract checks passed. Gradle/JVM tests were attempted but the Gradle wrapper download was blocked by the environment proxy (HTTP 403). Android instrumentation, emulator/runtime rendering and shaping, and physical-device validation remain unexecuted and unverified. Routing, diagnostic core, proximity behavior, controller/audio architecture, English/default and existing locale copy, shared Mini geometry/measured-fit architecture, and shared direction/layout architecture are unchanged.

- Added Uzbek (`uz`) as one neutral, complete LTR product locale in generic `values-uz` resources using Latin (`Latn`) script, intended to serve `uz`, `uz-UZ`, and `uz-Latn-UZ` with default identity `uz-Latn-UZ`; Uzbek Cyrillic is not covered and no regional, script-specific, or duplicate tree was added. The unchanged 28-key English source contract completed FULL linguistic review plus targeted mini-review at PASS — HIGH with no HOLDs; the supplied frozen resource set is preserved exactly. Frozen semantics distinguish ON `Yoniq` from runtime ACTIVE `Faol`, preserve the complete `Tayyor` / `Kutilmoqda` / `Faol` / `Xatolik` paradigm, and retain reviewed `AI`, collision-reviewed `Mini`, `Birlamchi`, `Kengaytirilgan sozlamalar`, final audio-output switching wording, and contextual built-in call-earpiece term `telefoningizning ichki quloq karnaychasi`; About uses the final `nutq audiosini` wording. Persistent contracts protect qualifier identity and duplicate/Cyrillic-tree absence, Latin script, high-risk exact literals, accessibility action parallelism, key/placeholder parity, and absence of an Uzbek compact-state override. Android instrumentation covers generated LocaleConfig discovery, actual localized-resource resolution and LTR configuration for all three intended tags, and production-equivalent shared 16f/15f/14f common-size Mini measurement. Static source/resource checks passed. Gradle/JVM tests were not executed because the Gradle wrapper download was blocked by the environment proxy. Android instrumentation, emulator/runtime rendering, and physical-device validation remain unverified. Routing, diagnostic core, proximity behavior, audio architecture, English/default and existing locale copy, shared Mini geometry/measured-fit architecture, and shared direction/layout architecture are unchanged.

- Added Burmese (`my`) as one neutral, complete LTR product locale in generic `values-my` resources using Myanmar (`Mymr`) script, intended to serve `my`, `my-MM`, and `my-Mymr-MM` with default identity `my-Mymr-MM` and without regional or BCP-47 duplicate trees. The unchanged 28-key English source contract completed FULL linguistic review plus targeted mini-review at PASS — HIGH with no HOLDs; the supplied frozen resource set is preserved exactly. Frozen semantics distinguish ON `ဖွင့်ထားသည်` from runtime ACTIVE `အသုံးပြုနေသည်`, preserve the complete `အဆင်သင့်` / `စောင့်နေသည်` / `အသုံးပြုနေသည်` / `အမှား` paradigm, and retain reviewed `အေအိုင်`, collision-reviewed `မီနီ`, `မူရင်း`, `အဆင့်မြင့်အပြင်အဆင်များ`, audio-output switching wording, diagnostic-report terminology, built-in call-earpiece term `တယ်လီဖုန်းနားခွက်`, and parallel Power on/off, Expand, and Close accessibility actions. Persistent contracts protect qualifier identity and duplicate-tree absence, native script, high-risk exact literals, key/placeholder parity, and absence of a Burmese compact-state override. Android instrumentation covers generated LocaleConfig discovery, actual localized-resource resolution and LTR configuration for all three intended tags, and production-equivalent shared 16f/15f/14f common-size Mini measurement; no compact override is added unless that measurement fails at 14f. Static source/resource checks passed. Gradle/JVM tests were not executed because the Gradle wrapper download was blocked by the environment proxy. Android instrumentation, emulator/runtime rendering, and physical-device validation remain unverified. Routing, diagnostic behavior, proximity behavior, audio architecture, English/default and existing locale copy, shared Mini geometry, and direction/layout architecture are unchanged.

- Added Odia (`or`) as one neutral, complete LTR product locale in generic `values-or` resources using native Odia/Orya script, intended to serve `or`, `or-IN`, and `or-Orya-IN` with the default identity `or-Orya-IN` and without regional or BCP-47 duplicate trees. The unchanged 28-key English source contract completed FULL linguistic review plus targeted mini-review at PASS — HIGH; the supplied frozen resource set is preserved exactly. Frozen semantics distinguish ON `ଚାଲୁ ଅଛି` from runtime ACTIVE `ସକ୍ରିୟ`, preserve the complete `ପ୍ରସ୍ତୁତ` / `ଅପେକ୍ଷାରତ` / `ସକ୍ରିୟ` / `ତ୍ରୁଟି` paradigm, retain reviewed `ଏଆଇ`, collision-reviewed `ମିନି`, `ଡିଫଲ୍ଟ`, and `ଉନ୍ନତ ସେଟିଂସ`, preserve the final audio-output switching wording, and identify the built-in call earpiece contextually as `ଆପଣଙ୍କ ଫୋନର ବିଲ୍ଟ-ଇନ୍ ହ୍ୟାଣ୍ଡସେଟ୍ ଇୟରପିସ୍`. Persistent contracts protect qualifier identity and duplicate-tree absence, native script, high-risk exact literals, accessibility action parallelism, key/placeholder parity, and absence of an Odia compact-state override. Android instrumentation covers generated LocaleConfig discovery, actual localized-resource resolution and LTR configuration for all three intended tags, and production-equivalent shared 16f/15f/14f common-size Mini measurement. Static source/resource checks passed. Gradle/JVM tests were not executed because the Gradle wrapper download was blocked by the environment proxy. Android instrumentation, emulator/runtime rendering, and physical-device validation remain unverified. Routing, diagnostic core, proximity behavior, audio architecture, English/default and existing locale copy, shared Mini geometry/measured-fit architecture, and shared direction/layout architecture are unchanged.

- Added Azerbaijani (`az`) as two independently reviewed, real user-facing variants. Northern Azerbaijani uses the generic `values-az` qualifier with default identity `az-Latn-AZ`, Latin script, and LTR direction; its frozen 28 stable strings are **PASS HIGH** with 0 FIX, 0 POLISH, and 0 HOLD. Iranian / South Azerbaijani uses `values-b+az+Arab+IR` with identity `az-Arab-IR`, Arabic script, and RTL direction; its frozen 28 stable strings are **PASS MEDIUM** with 0 FIX, 0 POLISH, and 0 HOLD, where MEDIUM records evidence and orthographic/software-standard consistency strength rather than a known translation defect. The temporary experimental `settings_fake_phone_pre_arm` key is intentionally excluded from both variants and continues to fall back to English. Cyrillic Azerbaijani is intentionally out of scope, and `azb` is linguistic evidence rather than an additional Android resource locale. Persistent contracts protect qualifier separation and unsafe-tree absence, exact stable-key and placeholder parity, Latin versus Arabic script, LTR versus RTL resolution, distinct ON (`Açıq` / `آچیق`) and ACTIVE (`Aktiv` / `فعال`) semantics, both natural four-state paradigms, AI, Mini, Settings, Default, Advanced, built-in call-earpiece, audio-output, diagnostic-report, accessibility wording, and the experimental-key exclusion. Android instrumentation covers generated LocaleConfig discovery; actual resource resolution for `az`, `az-AZ`, `az-Latn-AZ`, `az-IR`, `az-Arab`, and `az-Arab-IR`; direction for each tag; accessibility resource formatting; and independent production-equivalent 16f/15f/14f Mini measurement. Static/source-contract and JVM/Gradle results are recorded by this change's executed checks; Android instrumentation-selected Mini sizes, emulator/runtime rendering and locale switching, Arabic shaping and mixed-direction presentation, accessibility behavior, and physical-device validation remain unverified unless separately executed. No routing, audio, proximity, controller architecture, diagnostics-core behavior, Fake Phone behavior, shared Mini sizing/geometry, English/default copy, or existing frozen locale changed.

State reflects evidence, not aspiration.

## DONE

- Added Catalan (`ca`) as one neutral, complete LTR stable-product locale in generic `values-ca` resources using Latin (`Latn`) script, intended to serve `ca`, `ca-ES`, and `ca-Latn-ES` with default identity `ca-Latn-ES`; no Andorran, Balearic, Valencian, regional, script-specific, or duplicate resource tree was added. The frozen 28-string stable localization completed FULL review plus targeted MINI review at **PASS HIGH**, with 0 FIX, 0 POLISH, and 0 HOLD, and is preserved exactly. The temporary experimental `settings_fake_phone_pre_arm` key remains intentionally excluded under the repository-wide translated-resource scope. Frozen semantics distinguish ON `Activat` from runtime ACTIVE `Actiu`, preserve the full `A punt` / `En espera` / `Actiu` / `Error` paradigm, and protect reviewed `IA`, `Mini`, `Predeterminat`, `Configuració avançada`, `sortida d'àudio`, `informe de diagnòstic`, `auricular integrat del telèfon`, and accessibility/control wording. Persistent static/source contracts protect stable-key and placeholder parity, qualifier identity and duplicate-tree absence, exact high-risk literals, the temporary-key exclusion, and absence of a Catalan compact override. Android instrumentation covers generated LocaleConfig discovery, actual Android resource resolution and LTR configuration for all three intended tags, formatted accessibility text, and production-equivalent shared 16f/15f/14f common-size measurement of all four natural Mini labels. Static/source-contract and JVM/Gradle results are recorded by this change's executed checks; Android instrumentation, its selected Mini size, emulator/runtime rendering and locale switching, accessibility behavior, and physical-device presentation remain unverified unless separately executed. No routing, audio, proximity, controller, diagnostic-core, Fake Phone, English/default, existing locale, shared Mini sizing/geometry, or unrelated production behavior changed.

- Added Zulu / isiZulu (`zu`) as one neutral, complete LTR product locale in generic `values-zu` resources using Latin script, intended to serve both `zu` and `zu-ZA` with the default CLDR identity `zu-Latn-ZA` and without regional or BCP-47 duplicate trees. The unchanged 28-key English source contract completed FULL linguistic review plus targeted Ready and routing mini-reviews: Ready `Ilungile` was retained at HIGH confidence, while routing was polished from the earlier descriptive candidate to `Iyalinda ukushintsha okukhiphayo komsindo`. Frozen semantics retain international `AI`, distinguish ON `ivuliwe` from runtime ACTIVE `Iyasebenza`, preserve the complete `Ilungile` / `Iyalinda` / `Iyasebenza` / `Iphutha` paradigm, retain collision-reviewed `Mini` as distinct from native `imini`, and identify the built-in call earpiece as `isipikha sendlebe`. Persistent contracts protect qualifier identity and duplicate-tree absence, Latin script, high-risk exact literals, key/placeholder parity, and absence of a Zulu compact-state override. Android instrumentation covers generated LocaleConfig discovery, real `values-zu` resolution and LTR configuration for `zu` and `zu-ZA`, and production-equivalent shared 16f/15f/14f common-size Mini measurement. Source/static and JVM test results are recorded by this change's executed checks; Android instrumentation-selected size, runtime/emulator rendering and locale switching, and physical-device presentation remain unexecuted and unverified. Routing, diagnostic core, proximity behavior, audio architecture, English/default and existing locale copy, shared Mini geometry/measured-fit architecture, and shared direction/layout architecture are unchanged.

- Added Amharic (`am`) as one neutral, complete LTR product locale in generic `values-am` resources using native Ethiopic/Geʽez script, intended to serve both `am` and `am-ET` without regional or BCP-47 duplicate trees. The unchanged 28-key English source contract completed FULL linguistic review plus targeted Advanced and built-in call-earpiece mini-review; contemporary compact product copy retains `AI`. Frozen semantics distinguish ON (`በርቷል`) from runtime ACTIVE (`ገባሪ`), preserve the full `ዝግጁ` / `በመጠበቅ ላይ` / `ገባሪ` / `ስህተት` state paradigm, retain Mini as `ሚኒ`, and identify the built-in phone call receiver as `የስልክዎ አብሮገነብ መስሚያ` rather than headphones terminology. Persistent source contracts protect qualifier identity, Ethiopic script, high-risk terminology, exact key/placeholder parity, duplicate-tree absence, and absence of an Amharic compact-state override. Android instrumentation covers generated LocaleConfig discovery, real generic-resource resolution and LTR configuration for `am` and `am-ET`, and the production-equivalent shared 16f/15f/14f common-size Mini selector. Source-contract and static validation are complete; the instrumentation-selected common size, runtime/emulator glyph and presentation validation, and physical-device presentation remain pending unless separately executed. Routing, diagnostics, proximity behavior, existing locale copy, English/default copy, shared Mini geometry, and direction/layout architecture are unchanged.

- Added Hausa (`ha`) as one neutral, complete LTR product locale in generic `values-ha` resources for Latin/Boko Hausa, intended to serve `ha`, `ha-NG`, `ha-NE`, and `ha-GH` without regional or Ajami resource trees. The unchanged 28-key source contract completed FULL linguistic review plus targeted Default (`Na asali`), Advanced (`Ci gaba`), and Mini review. The native Hausa word `mini` ("to/for me") fails the lexical-collision gate, so the frozen compact-controller label is `Ƙarami` and its contextual form is `Ƙaramin mai sarrafa Private Audio`; contemporary compact product copy retains `AI`. Frozen semantics distinguish ON (`An kunna Private Audio`) from runtime ACTIVE (`Ana aiki`), preserve the full `A shirye` / `Ana jira` / `Ana aiki` / `Kuskure` state paradigm, and identify the built-in call receiver as `lasifikar kunne da ke cikin wayarka`. Persistent source contracts protect qualifier identity, Latin/Boko orthography, high-risk terminology, exact key/placeholder parity, and absence of Hausa regional, Ajami, or compact-state overrides. Android instrumentation covers generated LocaleConfig discovery, real generic-resource resolution and LTR configuration for all four intended tags, and the production-equivalent shared 16f/15f/14f common-size Mini selector. A Python XML/source validator passed XML parsing, duplicate-key absence, exact 28-key and placeholder parity, qualifier exclusions, and Mini-override absence; conflict-marker scanning and `git diff --check` also passed. Gradle JVM tests could not execute because the wrapper distribution download was rejected by the environment proxy, so instrumentation-selected common size, actual Android resource resolution, runtime/emulator rendering, and physical-device presentation remain unverified.

- Added Pashto (`ps`) as one neutral, complete RTL product locale in the standard `values-ps` qualifier, covering `ps`, `ps-AF`, and `ps-PK` without duplicate regional or BCP-47 resource trees. The unchanged 28-key English contract completed FULL linguistic review plus targeted Advanced (`پرمختللی`) and built-in call-receiver (`د تلیفون رسیدونکی`) review; the frozen copy retains `AI`, the collision-reviewed `مینی`, and distinct ON (`چالان`) versus ACTIVE (`فعال`) semantics. Persistent source contracts protect qualifier identity, native Pashto script/name, key and placeholder parity, the four-state paradigm, high-risk terminology, and absence of ambiguous speaker/headphone wording or a Pashto Mini override. Android instrumentation now covers generated LocaleConfig discovery, real generic-resource resolution and RTL for `ps`/`ps-AF`/`ps-PK`, and the production-equivalent shared 16f/15f/14f common-size Mini selector. JVM/source validation is recorded by the applicable test run; instrumentation-selected size and runtime/emulator/physical rendering remain unverified unless separately executed.

- Corrected a HIGH-confidence Urdu Mini lexical collision: `منی`, an unrelated sexual/body-fluid term in Urdu, is no longer used for the compact-controller concept. The reviewed HIGH-confidence replacement is `کمپیکٹ`; the natural spoken controller description now uses the same term while preserving its parallel Power, Expand, and Close actions. A mandatory universal transliterated-loanword lexical-collision gate was added to localization policy and workflow, including future Arabic-derived-script reviews. The focused current-corpus audit covered only `floating` and its controller-description equivalent: Arabic and both Punjabi scripts use native compact/minimized forms; Persian `مینی`, Hindi `मिनी`, Japanese `ミニ`, Korean `미니`, Russian `Мини`, Ukrainian/Belarusian `Міні`, Thai `มินิ`, and Chinese `迷你` remain PASS with HIGH confidence as established forms without a material collision; Latin `Mini`, including Hebrew and Yiddish resources, remains PASS with HIGH confidence. Urdu was the sole FIX (assessment and replacement confidence HIGH); no POLISH or HOLD finding was identified. A locale-specific semantic contract freezes both corrected Urdu fields and rejects `منی` only within those Mini/controller fields. This was a focused linguistic and source-contract audit, not runtime or physical-device validation.

- Added Punjabi as two independently FULL- and MINI-reviewed product locales: Punjabi (India), in Gurmukhi and LTR, uses logical tag `pa-Guru-IN` with Android qualifier `values-b+pa+Guru+IN`; Punjabi (Pakistan), in Shahmukhi and RTL, uses logical tag `pa-Arab-PK` with qualifier `values-b+pa+Arab+PK`. Both resources have complete parity with the unchanged 28-key English source, preserve `Private Audio` and `AI`, distinguish ON (`ਚਾਲੂ` / `چالو`) from ACTIVE (`ਸਰਗਰਮ` / `فعال`), use the reviewed compact-controller and built-in-call-earpiece terminology, and retain the reviewed Ready / Waiting / Active / Error paradigms through the shared Mini alias architecture without locale-specific overrides. A targeted Shahmukhi review uses Punjabi singular copula `اے` throughout and restores the approved Advanced label `ایڈوانسڈ`. Persistent source and Android instrumentation contracts protect logical identities, exact qualifiers, generated discovery, resource resolution, script direction, frozen semantics, and all four resolved Mini labels and their shared-slot measurements. Static XML, duplicate-key, exact-key, and placeholder contracts passed; the Gradle unit/lint/assembly run was attempted but its wrapper download was blocked by the environment proxy (HTTP 403), so the Android instrumentation measurement contract and runtime/emulator/physical-device rendering, glyph shaping, bidi presentation, interaction, and Mini fit remain unexecuted and unverified. Existing routing, diagnostics, proximity, and direction-driven RTL implementation are unchanged.

- **2026-08-20 localization corpus audit baseline:** Independent high-risk PASS A review and qualifier/resource-resolution review covered the current corpus of 63 string resource sets (the default English source plus 62 localized resource directories). Full 28-string PASS B review covered `zh-Hans`, `zh-Hant`, `hi`, `it`, `ro`, and `sw`; a targeted Icelandic earpiece mini-review retained `innbyggða símahátalarann` as valid Android/Telephony and native-product call-earpiece terminology. Seven approved corrections are implemented: Simplified and Traditional Chinese ACTIVE `使用中`, Hindi ACTIVE `सक्रिय`, Italian ON `Private Audio è attivato`, Romanian ON `Private Audio este activat`, Swahili ACTIVE `Inatumika`, and Swahili Advanced Settings `Mipangilio ya kina`. These restore ON/enabled versus runtime ACTIVE semantics and established Swahili Settings terminology; durable locale contracts protect the accepted and rejected forms, the Icelandic reviewed-no-change phrase remains protected, and the static Mini contract now explicitly couples instrumentation measurement assumptions to production's unchanged 16f sans-serif normal status paint. Localization policy and skill guidance now clarify first-party terminology as preferred, reviewable evidence; recognize natural Latin, local-script, transliterated, and native `Mini` forms; and require reuse of this baseline unless a concrete invalidation trigger warrants targeted or explicitly requested corpus review. All other audited unchanged locales had no actionable FIX, POLISH, or HOLD finding under the current source contract. This is linguistic/audit and source-contract evidence, not physical runtime validation of every locale, and it does not prevent future targeted review. Static XML/key/placeholder/duplicate/scope checks passed; Gradle unit execution was attempted but the wrapper download was blocked by the environment proxy (HTTP 403), so Android runtime and physical-device validation were not performed.

- Fixed the Indonesian locale failure exposed by a physical Android 13 test: selecting logical locale `id` showed `Indonesia`, but the `values-id` resource tree fell back to English on that runtime. The unchanged 28-key Indonesian resource moved to Android's API 31+ compatible legacy alias `values-in` while product identity remains modern `id`; its sole copy change restores the ON-versus-ACTIVE distinction as `Private Audio diaktifkan` versus `Aktif`. Layer 4.1 now protects the `id`/`in`, `he`/`iw`, and `yi`/`ji` logical/resource mappings, unsafe modern-folder absence, selector-relevant native-language behavior, and frozen Indonesian semantics. A focused instrumentation contract requests the modern tags and exercises real Android resource resolution for all three aliases. Localization policy and the repository skill now require alias research and qualifier/resolution coverage. Automated Android resource-resolution execution and post-fix physical Indonesian verification remain pending.

- Physical-device evidence demonstrated that the full Tamil and Gujarati WAITING labels ellipsized in Mini. Mini now uses generic compact-state resource aliases that resolve to each locale's own full Ready / Waiting / Active / Error strings by default, with independently reviewed frozen Tamil (`தயார் / காத்திருப்பு / செயலில் / பிழை`) and Gujarati (`તૈયાર / પ્રતીક્ષા / સક્રિય / ભૂલ`) visual overrides. Their natural full state resources remain unchanged for Main and Mini accessibility speech. The single shared Mini geometry remains 300 × 62 with its status slot minimally increased from 92 to 100 design units; no locale-specific runtime or geometry branch was added. Source and on-device measurement contracts protect resource fallback, semantics, accessibility separation, shared geometry, and Tamil compact WAITING fit with the current Mini paint. Runtime rendering and physical-device re-validation remain required.

- Added Bengali (`bn`) as a complete, independently FULL linguistically reviewed and MINI adversarial stylistically reviewed frozen LTR product locale in standard `values-bn` Android resources. Its 28 keys and format placeholders align with the unchanged English fallback, and Android's generated locale configuration discovers it by the native/display name `বাংলা`. An independent persistent Layer 4.1 contract protects Bengali-script content, the reviewed `AI` terminology, ON-versus-ACTIVE distinction, audio-output switching, device/system-language and system-default distinctions, screen-proximity semantics, built-in handset-earpiece wording, parallel accessibility actions, and all four shared Mini states. This additive localization change is source-contract validated; Android runtime locale switching, rendering, and Mini presentation remain unverified.

- Added Basque (`eu`), Albanian (`sq`), and Latvian (`lv`) as complete, independently FULL-reviewed and frozen LTR product locales in standard Android resources; Basque's built-in call-earpiece terminology additionally completed MINI review. Basque now uses the reviewed native artificial-intelligence abbreviation `AA`, and Latvian uses the reviewed native abbreviation `MI`; durable locale-specific semantic assertions protect both choices. Each locale's 28 keys and format placeholders align with the unchanged English fallback, and Android's generated locale configuration discovers all three by their native locale names without a parallel registry or locale-specific application logic. Separate persistent locale-specific semantic contract tests protect each locale's native name, frozen audio-direction wording, device/system language distinction, screen-specific proximity action, built-in call-earpiece terminology, grammatically parallel Power/Expand/Close actions, and four frozen Mini states; Albanian additionally protects the reviewed `është ndezur` ON notification independently from the `Aktiv` runtime state. Mini continues to use the shared state resources under the existing measured single-line ellipsizing architecture. Existing locale copy, production code, routing, diagnostics, proximity, UI geometry, typography, Gradle configuration, and runtime behavior remain unchanged. Source-contract validation covers XML validity, duplicate/key/placeholder parity, discovery, and frozen semantics; Android runtime locale switching, rendering, and Mini presentation remain unvalidated.

- Added Finnish (`fi`), Estonian (`et`), and Hungarian (`hu`) as complete, independently linguistically reviewed and frozen LTR product locales in standard Android resources. Each locale's 28 keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers all three by their native locale names without a parallel registry or locale-specific application logic. Separate persistent locale-specific semantic contract tests protect each locale's native name, frozen audio-direction wording, device/system language distinction, screen-specific proximity action, built-in call-earpiece terminology, grammatically parallel Power/Expand/Close actions, and four frozen Mini states. Mini continues to use the shared state resources under the existing measured single-line ellipsizing architecture. Existing locale copy, production code, routing, diagnostics, proximity, UI geometry, typography, Gradle configuration, and runtime behavior remain unchanged. Source-contract validation covers XML validity, duplicate/key/placeholder parity, discovery, and frozen semantics; Android runtime locale switching, rendering, and Mini presentation remain unvalidated.

- Added Swedish (`sv`), Norwegian Bokmål (`nb`), Danish (`da`), Icelandic (`is`), and Faroese (`fo`) as complete, independently FULL + MINI reviewed and frozen LTR product locales in standard Android resources. Each locale's 28 keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers all five by their native locale names without a parallel registry or locale-specific application logic. Persistent locale-specific semantic contract assertions now protect every locale's native name, frozen routing and language terminology, screen-specific proximity behavior, parallel Power/Expand/Close actions, built-in call-earpiece terminology, and four reviewed shared Mini states. Mini continues to use the full shared state resources under the existing measured single-line ellipsizing architecture. Existing locale copy, production code, routing, diagnostics, runtime behavior, UI geometry, and typography remain unchanged; source-contract checks are automated, while Android runtime locale switching, rendering, and Mini presentation remain unvalidated.

- Added standard Italian (`it`) and Romanian (`ro`) as complete, independently reviewed LTR product locales in Android `values-it` and `values-ro` resources. Each locale's 28 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `it` as `Italiano` and `ro` as `Română` for the existing per-app language selector without a parallel registry or locale-specific application logic. Both localizations preserve natural audio-redirection wording, the screen-specific proximity action, the system/device distinction, and unambiguous built-in call-earpiece terminology. Their accessibility controller actions are grammatically parallel, and Mini uses each locale's full shared status resources under the existing measured single-line ellipsizing architecture. Existing locale copy, routing, diagnostics, controller/service behavior, proximity mechanics, RTL architecture, runtime logic, and UI geometry remain unchanged; runtime Android locale switching, presentation, and Mini validation remain required.

- Added standard Malay (`ms`) as a complete LTR product locale in Android `values-ms` resources. All 28 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `ms` for the existing per-app language selector as `Melayu` without a parallel registry or Malay-specific application logic. The linguistically reviewed Malay copy preserves contemporary Malay orthography, Latin-script `AI`, natural audio-redirection wording, the unambiguous built-in call earpiece term, the screen-specific proximity action, and the system/device language distinction. Its accessibility controller actions use parallel verb forms, and Mini uses the full shared status resources (`Sedia`, `Menunggu`, `Aktif`, and `Ralat`) under the existing measured single-line ellipsizing architecture. Existing locale copy, routing, diagnostics, controller/service behavior, proximity mechanics, RTL architecture, runtime logic, and UI geometry remain unchanged; runtime Android locale switching, presentation, and Mini validation remain required.

- Added standard Kannada (`kn`) as a complete LTR product locale in Android `values-kn` resources. All 28 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `kn` for the existing per-app language selector as `ಕನ್ನಡ` without a parallel registry or Kannada-specific application logic. The linguistically reviewed Kannada copy preserves contemporary Kannada script and combining marks, Latin-script `AI`, the approved audio-switching concept, the unambiguous phone `ಇಯರ್‌ಪೀಸ್` term, the screen-specific proximity action, and the system/device language distinction. Its accessibility controller actions use parallel infinitive forms, and Mini uses the full shared status resources (`ಸಿದ್ಧ`, `ಕಾಯುತ್ತಿದೆ`, `ಸಕ್ರಿಯ`, and `ದೋಷ`) under the existing measured single-line ellipsizing architecture. Existing locale copy, routing, diagnostics, controller/service behavior, proximity mechanics, RTL architecture, runtime logic, and UI geometry remain unchanged; runtime Android glyph rendering, locale switching, Main and Settings layout, and Mini ellipsizing/paint-bound validation remain required.

- Added standard Gujarati (`gu`) as a complete LTR product locale in Android `values-gu` resources. All 28 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `gu` for the existing per-app language selector as `ગુજરાતી` without a parallel registry or Gujarati-specific application logic. The linguistically reviewed Gujarati copy preserves contemporary Gujarati script and combining marks, Latin-script `AI`, the approved audio-switching concept, the unambiguous phone `ઇયરપીસ` term, the screen-specific proximity action, and the system/device language distinction. Its accessibility controller actions use parallel infinitive forms, and Mini uses the full shared status resources (`તૈયાર`, `રાહ જોઈ રહ્યું છે`, `સક્રિય`, and `ભૂલ`) under the existing measured single-line ellipsizing architecture. Existing locale copy, routing, diagnostics, controller/service behavior, proximity mechanics, RTL architecture, runtime logic, and UI geometry remain unchanged; runtime Android glyph rendering, locale switching, Main and Settings layout, and Mini ellipsizing/paint-bound validation remain required.

- Reconciled the routing compatibility foundation on the primary Xiaomi `2201117TY` (Android 13/API 33): automatic provider-independent `COMMUNICATION`, `ASSISTANT`, and `BROWSER_COMMUNICATION` classifiers remain distinct and converge on one protected POC-5 body. Supplied physical evidence covers ChatGPT, Gemini Live, Grok, Perplexity Android, Character.AI Calls, and ChatGPT Web in Chrome and Mi Browser; it does not establish other OEMs, Android versions, Gecko, or all Chromium browsers. The user separately reported local `./gradlew testDebugUnitTest lintDebug assembleDebug` PASS after the earlier remote Gradle-download limitation; this is supplied local verification, not a Codex-executed result.

- Added standard Marathi (`mr`) as a complete LTR product locale in Android `values-mr` resources. All 28 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `mr` for the existing per-app language selector as `मराठी` without a parallel registry or Marathi-specific application logic. The linguistically reviewed Marathi copy preserves contemporary Devanagari script and combining marks, Latin-script `AI`, the approved `ऑडिओ स्विच` audio-switching concept, the unambiguous phone `इयरपीस` term, the screen-specific proximity action, and the system/device language distinction. Its accessibility controller actions use parallel verbal-noun forms, and Mini uses the full shared status resources (`तयार`, `प्रतीक्षा`, `सक्रिय`, and `त्रुटी`) under the existing measured single-line ellipsizing architecture. Existing locale copy, routing, diagnostics, controller/service behavior, proximity mechanics, RTL architecture, runtime logic, and UI geometry remain unchanged; runtime Android glyph rendering, locale switching, Main and Settings layout, and Mini ellipsizing/paint-bound validation remain required.

- Promoted the physically validated browser-communication path to automatic detection whenever Private Audio is ON, removing its Advanced Settings switch, persisted preference, test tag, and localized resource from every locale. The exact `MODE_IN_COMMUNICATION` + built-in speaker + `USAGE_VOICE_COMMUNICATION`/`CONTENT_TYPE_UNKNOWN` trigger remains independent from the unchanged communication and assistant classifiers, and all three distinct origins enter the same protected POC-5 body with exactly one request. Browser lifecycle tracking still uses only the unknown-content contribution and the existing 1.5-second confirmation before unchanged cleanup and Waiting re-arm. Supplied physical results establish ChatGPT Web in Chrome routing, cleanup, and subsequent sessions without restarting Private Audio; ChatGPT Android and Gemini Live regressions pass, ordinary media and assistant sonification remain ignored, and Power OFF cleanup remains unchanged.

- Promoted the physically validated assistant-class path to automatic detection whenever Private Audio is ON, and removed the experimental Gemini setting, persistence, test tag, and localized resource from every locale. The communication trigger remains unchanged and logically separate from the exact `USAGE_ASSISTANT` + `CONTENT_TYPE_SPEECH` trigger; both select an internal origin and enter the same protected POC-5 body with its one-request guard. Supplied physical results establish Gemini Live speaker-to-earpiece routing, proximity, cleanup, Waiting re-arm, and the existing ChatGPT regression path on the tested device. Automated contracts cover media and assistant-sonification exclusion, shared sequencing, one request, and cycle-specific cleanup; unreported YouTube, NotebookLM, and Kimi executions remain physical regression work.

- Added standard Telugu (`te`) as a complete LTR product locale in Android `values-te` resources. All 28 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `te` for the existing per-app language selector as `తెలుగు` without a parallel registry or Telugu-specific application logic. The reviewed Telugu product copy preserves contemporary Telugu script and combining marks, Latin-script `AI`, `ఆడియోను మార్చ` audio-switching wording, the unambiguous `ఫోన్ ఇయర్‌పీస్` call-earpiece term, the screen-specific proximity action, and the system/device language distinction. Mini uses the full shared status resources (`వేచి ఉంది` and `యాక్టివ్`) under the existing measured single-line ellipsizing architecture. Routing, diagnostics, authoritative state, controller semantics, Main/Settings/Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android glyph rendering, locale switching, Main and Settings layout, and Mini ellipsizing/paint-bound validation remain required.
- Unified Main and Mini state motion without changing their geometry, colors, labels, controls, or authoritative state projection: READY and ERROR are static; WAITING uses a calm 1.8-second breathing cadence, with Main synchronizing its existing Power glow and status dot while Mini animates only its dot; ACTIVE keeps Main Power/glow stable and gives only the Main/Mini status dot a 1.4-second pulse. Static states do not retain infinite animation work, and Mini cancels its animator when observation ends. Automated contracts cover the semantic split and cleanup, but physical-device QA remains pending for WAITING cadence, WAITING → ACTIVE transition, ACTIVE dot prominence, Main/Mini consistency, and whether the motion feels calm rather than distracting.
- Increased only Main's inner Power glyph by 1.04× around its existing center, scaling the colored arc, vertical stem, their strokes, and enabled glow together while leaving the outer control diameter, border, position, click target, colors, animation, and state semantics unchanged. Physical-device screenshot QA remains required for final optical approval.
- Refined the repo-local localization skill with a proportional source-to-target semantic back-check, reverse-gloss sanity check for high-risk strings, parallel-action review for spoken accessibility/control descriptions, and a stronger risk-scaled final linguistic PASS gate. The authoritative localization policy, product copy, translations, locale resources, application code, and runtime behavior remain unchanged.
- Added standard Tamil (`ta`) as a complete LTR product locale in Android `values-ta` resources. All 28 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `ta` for the existing per-app language selector as `தமிழ்` without a parallel registry or Tamil-specific application logic. The reviewed Tamil product copy preserves contemporary Tamil script and combining marks, Latin-script `AI`, `ஆடியோவை மாற்ற` audio-switching wording, and the unambiguous `இயர்பீஸ்` call-earpiece term. Mini uses the full shared status resources (`காத்திருக்கிறது` and `செயலில் உள்ளது`) because the current locale-independent architecture has no separate compact status keys; its existing measured single-line ellipsizing remains the layout safeguard. Routing, diagnostics, authoritative state, controller semantics, Main/Settings/Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android glyph rendering, system-default and explicit locale switching, Main and Settings layout, and Mini ellipsizing/paint-bound validation remain required.
- Added the repo-local `private-audio-localization` Codex skill to operationalize the authoritative localization policy, and updated `AGENTS.md` to route localization-impacting work through that policy, the skill, and a current source/locale inventory review. The durable policy now records concise linguistic-audit lessons, and the general workflow links to the specialized procedure. No product copy, translations, locale resources, application code, Gradle configuration, or runtime behavior changed.
- Applied the focused linguistic review of the proximity-screen Settings label: the English fallback, Polish, and Nigerian Pidgin copy now use the approved concise wording, while Persian and every other reviewed locale remain unchanged. Key and placeholder parity, locale discovery, application behavior, routing, diagnostics, authoritative state, proximity behavior, Mini, RTL infrastructure, typography, and UI geometry remain unchanged.
- Added standard Filipino (`fil`) as a complete LTR product locale in Android `values-fil` resources. All 28 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `fil` for the existing per-app language selector as `Filipino` without a parallel registry or Filipino-specific application logic. The approved contemporary Filipino/Taglish terminology, Latin-script `AI`, `ilipat ang audio` action, `earpiece ng telepono` distinction, and full Mini statuses are preserved. Routing, diagnostics, authoritative state, controller semantics, Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android rendering, locale switching, Main bottom-action layout, and Mini ellipsizing validation remain required.
- Follow-up physical-device-informed optical compensation raises the Main bottom action baseline for visible text containing a Unicode Arabic-script code point from 14 sp to 15 sp, while all other scripts retain 13 sp. The existing 112 dp width, 16 sp line height, 88 dp control geometry, natural one-/two-line behavior, and measured 0.5 sp fallback to 11 sp remain authoritative and component-local. Detection remains text/script-based rather than locale- or RTL-based, so current Arabic, Persian, Urdu, mixed-script labels, and future Arabic-script locales share the rule without a registry. Translation resources and all other typography, geometry, icons, routing, service, Settings, and Mini behavior are unchanged; final Polish/Arabic/Persian/Urdu physical-device typography comparison remains pending.
- Fixed Mini gesture arbitration so a drag can begin anywhere on its surface, including Power, Expand, and Close. A simple tap still activates the intended control only when release resolves to that control; movement beyond Android touch slop permanently converts the gesture to a drag and cancels the pending control action. Existing LTR/RTL hit geometry, overlay movement/clamping, dimensions, visuals, and control behavior are unchanged. Automated JVM contracts cover every start region, threshold boundaries, irreversible drag conversion, UP/CANCEL safety, and ordinary taps; physical-device touch validation remains required.
- Added standard Thai (`th`) as a complete LTR product locale in Android `values-th` resources. All 28 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `th` for the existing per-app language selector as `ไทย` without a parallel registry or Thai-specific application logic. The approved contemporary terminology, Thai orthography and combining marks, Latin-script `AI`, `สลับเสียง` wording, `หูฟังด้านบนของโทรศัพท์` call-earpiece distinction, and full Mini statuses are preserved. Routing, diagnostics, authoritative state, controller semantics, Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android rendering, glyph and line-height inspection, locale switching, and Mini ellipsizing validation remain required.
- Established [`LOCALIZATION.md`](LOCALIZATION.md) as the single authoritative product-language policy and maintenance workflow. It records Android resources and generated locale discovery as the source architecture; native terminology, script/orthography, variants, earpiece/routing wording, surface-specific compact copy, shared RTL, system-font, diagnostics, placeholder, and CLDR plural guardrails; controlled all-locale/batch maintenance; contract expectations; evidence-level distinctions; and concise Codex checklists. This documentation-only change does not claim runtime validation and changes no application code, resources, translations, locales, tests, Gradle configuration, routing, diagnostics, or UI behavior.
- Added the accepted, locale-independent Mini bidirectional-layout and measured localized-text fitting rules to the decision log as D-023 and D-024. They apply to all current and future locales without changing implementation, overlay positioning, drag coordinates, UI geometry, routing, or state semantics.
- Completed the Layer 7C implementation audit and documentation closure with zero production-code changes. The repository retains one observer → service-owned state/preference eligibility decision → mechanics-only proximity-controller chain; all six eligibility inputs, centralized release on evidence departure, explicit Power OFF/service-destruction fail-safes, successive-cycle re-entry, diagnostic fields, default-ON persistence, UI isolation, and protected routing call counts remain covered. Layer 7 implementation is DONE; unrecorded physical OEM/accessory/lifecycle cases remain regression coverage rather than claimed PASS evidence.
- Recorded the core Layer 7 physical behavior as **FACT**: on 2026-08-18, Xiaomi `2201117TY` running Android 13/API 33 turned the screen off when moved near during `ACTIVE` built-in-earpiece routing and restored it automatically when moved far. Android reported wake-lock support `true`, and a post-session report showed held `false`. The tester's later overall statement that Layer 7B worked as intended is not attributed to any unrecorded individual matrix case.
- Fixed the Layer 7B Kotlin/JVM signature collision by naming the explicit service operation `updateProximityFeatureEnabled`, while retaining the authoritative readable property, its private setter, persistence, and immediate proximity synchronization. Call sites and focused contracts use the renamed operation; routing and session behavior are unchanged.
- Implemented Layer 7B proximity hardening and opt-out. The service now owns one persisted, default-ON “Turn screen off near ear” preference, incorporates it into the existing single eligibility decision, synchronizes changed values immediately, and reports it in existing diagnostics. Advanced Settings contains a localized accessible switch across every supported locale. OFF affects only proximity ownership; protected routing, session detection, product state, audio mode, silent track, Main/Floating lifecycle ownership, and cleanup ordering are unchanged. Focused automated contracts are not physical sensor evidence.
- Recorded partial Layer 7A physical evidence from 2026-08-18 on Xiaomi `2201117TY`, Android 13/API 33: during `ACTIVE` on the built-in earpiece, near turned the screen off and far restored it automatically; wake-lock support reported `true`, and the post-session report reported held `false`. This does not establish any other release boundary or complete the Layer 7 gate.

- Added standard Turkish (`tr`) as a complete LTR product locale in Android `values-tr` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `tr` for the existing per-app language selector as `Türkçe` without a parallel registry or Turkish-specific application logic. The approved contemporary terminology, Turkish orthography, Latin-script `AI`, `ses geçişi` wording, `ahize` call-earpiece distinction, and compact Mini statuses are preserved. Routing, diagnostics, authoritative state, controller semantics, Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android rendering, glyph inspection, locale switching, and Mini layout validation remain required.
- Added standard Vietnamese (`vi`) as a complete LTR product locale in Android `values-vi` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `vi` for the existing per-app language selector as `Tiếng Việt` without a parallel registry or Vietnamese-specific application logic. The approved contemporary terminology, Vietnamese diacritics, Latin-script `AI`, `chuyển đổi âm thanh` routing wording, `loa trong` call-earpiece distinction, and full `Đang hoạt động` Mini status are preserved. Routing, diagnostics, authoritative state, controller semantics, Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android rendering, glyph and line-height inspection, locale switching, and Mini layout validation remain required.
- Added standard Persian (`fa`) as the third production RTL locale in Android `values-fa` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `fa` for the existing per-app language selector as `فارسی` without a parallel registry or Persian-specific RTL logic. The approved contemporary Iranian Persian terminology, Persian orthography, native `هوش مصنوعی`, upper-phone-speaker distinction, and compact Mini statuses are preserved. Routing, diagnostics, authoritative state, controller semantics, Mini geometry, typography, colors, icons, spacing, existing locale copy, and the shared RTL architecture remain unchanged; runtime Android RTL rendering, glyph shaping, locale switching, Mini stability, mixed-direction text, and notification refresh validation remain required.

- Fixed the Mini controller issue identified by Arabic real-device QA: its unchanged physical 300 × 62 dp overlay/window position and drag coordinates remain left-origin screen coordinates, while its internal composition now follows Android layout direction. LTR retains STATUS | POWER | EXPAND | CLOSE exactly; every current or future RTL locale automatically receives CLOSE | EXPAND | POWER | STATUS, including mirrored visual and touch geometry, a mirrored directional Expand glyph, and a compact RTL-aware status whose dot is on the logical-start/right side. In-place configuration changes refresh direction and localized presentation without restarting or repositioning the overlay. Automated contracts cover the existing English/Polish LTR and Arabic/Urdu RTL resources; post-fix physical rendering and interaction remain unverified.
- Added standard Korean (`ko`) as a complete LTR product locale in Android `values-ko` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `ko` for the existing per-app language selector as `한국어` without a parallel registry or Korean-specific application logic. The approved contemporary terminology, Latin-script `AI`, call-earpiece distinction, and compact Mini statuses are preserved. Routing, diagnostics, authoritative state, controller semantics, Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android rendering, glyph inspection, locale switching, and Mini layout validation remain required.
- Fixed Main's localized bottom-action labels without changing translation wording or control geometry: each label may use a bounded width beyond its 88 dp control column, single-word actions remain on one line, multi-word actions may wrap at whitespace to at most two lines, and a shared measurement-driven fallback reduces the normal 13 sp size only as needed to a conservative 11 sp minimum. No ellipsis or locale-specific sizing was introduced; runtime visual validation remains required.
- Reconciled the Japanese/Kiswahili localization merge hotspot: all translated resource directories now receive the same key and placeholder parity checks through test-local directory discovery, while meaningful locale-specific wording and native-name assertions remain explicit. This removes the shared locale inventory/parity lists from future locale additions without changing translations, generated locale configuration, production localization, UI, routing, diagnostics, controller state, Mini behavior, or RTL preparation.
- Implemented the Layer 7A production foundation: `PrivateAudioService` now synchronously derives proximity-screen eligibility from its existing authoritative state and observer evidence, while one mechanics-only helper owns a supported, non-reference-counted public proximity wake lock. Ownership is idempotently acquired only for current `ACTIVE` + `MODE_IN_COMMUNICATION` + built-in-earpiece evidence and released on every eligibility departure, with explicit Power OFF and service-destruction fail-safes. Diagnostics record support, held state, last transition reasons/state/route. No UI, settings, sensor handling, polling, routing request, routing cleanup, mode, silent-track, session-boundary, or retry behavior changed. Xiaomi Android 13 near/off and far/on behavior is now physically confirmed; physical Power-button behavior remains **UNKNOWN / NOT TESTED**.
- Added standard Japanese (`ja`) as a complete LTR product locale in Android `values-ja` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `ja` for the existing per-app language selector as `日本語` without a parallel registry or Japanese-specific application logic. The approved contemporary terminology, Latin-script `AI`, physical-earpiece distinction, and compact Mini statuses are preserved. Routing, diagnostics, authoritative state, controller semantics, Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android rendering, glyph inspection, locale switching, and Mini layout validation remain required.
- Added standard Kiswahili (`sw`) as a complete LTR product locale in Android `values-sw` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `sw` for the existing per-app language selector as `Kiswahili` without a parallel registry or Kiswahili-specific application logic. The approved contemporary terminology, Latin-script `AI`, built-in earpiece distinction, and compact Mini statuses are preserved. Routing, diagnostics, authoritative state, controller semantics, Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android rendering, locale switching, and Mini presentation validation remain required.
- Added Nigerian Pidgin (`pcm`) as a complete LTR product locale in Android `values-pcm` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `pcm` for the existing per-app language selector as `Naijíriá Píjin` without a parallel registry or locale-specific application logic. If quantity/count strings are added later, `pcm` must be audited independently using Android `plurals` and its CLDR plural categories; English plural behavior must not be copied by assumption. Routing, diagnostics, authoritative state, controller semantics, Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android rendering and locale-switching validation remains required.
- Added standard Urdu (`ur`) as the second production RTL locale in Android `values-ur` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback; Android's generated locale configuration discovers `ur` for the existing per-app language selector as `اردو` without a parallel registry or Urdu-specific RTL logic. The approved contemporary Pakistani software terminology, Latin-script `AI`, built-in earpiece distinction, and compact Mini statuses are preserved. Routing, diagnostics, authoritative state, controller semantics, Mini geometry, typography, colors, icons, spacing, and the existing shared RTL architecture remain unchanged; runtime Android RTL rendering, glyph shaping, locale switching, Mini stability, and notification refresh validation remain required.
- Fixed and runtime-checked the Language child page without changing Settings styling or locale architecture: its option rows scroll within the visible safe-drawing area, all locale options remain reachable, and an accepted locale selection immediately returns to the still-open Settings root. The user confirmed this Language behavior works at runtime. Explicit child Back behavior and all other Settings pages remain unchanged; this evidence does not validate unrelated Settings, floating, or routing behavior.
- Added standard Indonesian (`id`) as a complete LTR product locale in Android `values-id` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `id` for the existing per-app language selector as `Bahasa Indonesia` without a parallel registry or Indonesian-specific application logic. Routing, diagnostics, authoritative state, controller semantics, Mini geometry and behavior, UI styling, existing locale copy, and RTL preparation remain unchanged; runtime Android rendering and locale-switching validation remains required.
- Prepared the existing presentation architecture for RTL locales without adding a locale: Settings forward/back chevrons now follow inherited Compose layout direction while neutral icons remain unchanged; the Mini retains its fixed STATUS → POWER → EXPAND → CLOSE order but uses a physical-left WindowManager coordinate basis and a measured, bidi-aware, single-line ellipsized status layout inside the existing status region; and an active foreground notification is reposted with newly resolved localized title/body text after configuration changes without restarting its service, controller, or routing. Mini dimensions, geometry, hitboxes, styling, routing, diagnostics, and authoritative state remain unchanged. Emulator/physical LTR → RTL → LTR rendering, dragging, notification, and Arabic shaping validation remain required with the subsequently added Arabic locale.
- Added standard Arabic (`ar`) as the first production RTL locale in Android `values-ar` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `ar` for the existing per-app language selector as `العربية` without a parallel registry or Arabic-specific application logic. This locale now exercises the existing RTL-ready Main, Settings, Mini, notification-refresh, and bidi-aware presentation architecture; runtime Android validation of RTL layout, Mini position and status rendering, notification refresh, mixed-direction text, and Arabic glyph shaping remains required. Routing, diagnostics, authoritative state, controller semantics, Mini geometry, typography, icons, colors, and spacing remain unchanged.

- Added Hindi (`hi`) as a complete product locale in standard Android `values-hi` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `hi` for the existing per-app language selector without a parallel registry or Hindi-specific application logic. Contemporary Hindi terminology, Devanagari AI wording, and the built-in earpiece distinction follow the approved product copy. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device rendering and system-locale validation remain required.

- Added Simplified Chinese (`zh-Hans`) and Traditional Chinese (`zh-Hant`) as complete, independently localized product locales using Android BCP 47 resource qualifiers `values-b+zh+Hans` and `values-b+zh+Hant`. Each locale's 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers both script-specific locales for the existing per-app language selector without a parallel registry or Chinese-specific application logic. Mainland/Simplified and Taiwan-oriented Traditional terminology remain intentionally distinct. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device rendering and system-locale validation remain required.
- Added Brazilian Portuguese and European Portuguese as complete, independently localized product locales in standard Android `values-pt-rBR` and `values-pt-rPT` resources. Each locale's 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `pt-BR` and `pt-PT` for the existing per-app language selector without a parallel registry or Portuguese-specific application logic. Variant-specific vocabulary and grammar remain distinct. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device validation remains required.
- Added Spanish as a complete product locale in standard Android `values-es` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `es` for the existing per-app language selector without a parallel registry or Spanish-specific application logic. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device validation remains required.
- Added French as a complete product locale in standard Android `values-fr` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `fr` for the existing per-app language selector without a parallel registry or French-specific application logic. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device validation remains required.
- Added Russian as a complete product locale in standard Android `values-ru` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `ru` for the existing per-app language selector without a parallel registry or Russian-specific application logic. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device validation remains required.
- Added Lithuanian as a complete product locale in standard Android `values-lt` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `lt` for the existing per-app language selector without a parallel registry or Lithuanian-specific application logic. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device validation remains required.
- Added Belarusian as a complete product locale in standard Android `values-be` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `be` for the existing per-app language selector without a parallel registry or Belarusian-specific application logic. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device validation remains required.
- Added Ukrainian as a complete product locale in standard Android `values-uk` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and existing translations; Android's generated locale configuration discovers `uk` for the existing per-app language selector without a parallel registry or Ukrainian-specific application logic. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device validation remains required.
- Added Czech and Slovak as complete, independently reviewed product locales in standard Android `values-cs` and `values-sk` resources. Each locale's 27 product-facing keys and format placeholders align with the unchanged English fallback, Polish, and German translations; Android's generated locale configuration discovers both locales for the existing per-app language selector without a parallel registry or locale-specific application logic. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device validation remains required.
- Added German as the next complete product locale in standard Android `values-de` resources. All 27 product-facing keys and format placeholders align with the unchanged English fallback and Polish translation; Android's generated locale configuration discovers `de` for the existing per-app language selector without a parallel registry or German-specific application logic. Routing, diagnostics, controller state, Mini behavior, and UI layout remain unchanged; device validation remains required.
- Real per-app language selection is implemented for Android 13/API 33 and later with the platform `LocaleManager`: Default clears the application locale override, supported choices are discovered from Android's generated `LocaleConfig`, and native Unicode locale names are derived from each configured locale rather than duplicated in an application registry. Polish remains the first translated locale and Android resources/generated locale config remain authoritative. Android 12/API 31–32 safely continue following system language because the platform per-app locale API is unavailable; adding a compatibility persistence framework remains an explicit future decision rather than part of this localization-only change. The existing Mini window handles configuration changes by refreshing its resource-backed state label and accessibility description in place, preserving window ownership, position, service binding, routing state, and controls. Device validation remains required.
- Added the first complete translated locale in standard Android `values-pl` resources and finalized the remaining approved English source-copy changes for the Main subtitle, foreground notification waiting text, and Default language label. Resource keys and placeholders match the English fallback; diagnostic internals remain English-only. The subsequent per-app language work above activates this foundation.
- Layer 6.2 floating-controller polish implemented: Expand reveals the existing Main task and immediately removes the overlay without changing controller/routing state. The unchanged 300 × 62 dp controller now presents STATUS → POWER → EXPAND → CLOSE, with status left, Power centered, right-side Expand and Close, and no separator; existing colors, drag behavior, and control touch targets remain. Emulator/physical interaction and task validation is required.
- Enabled app-module `BuildConfig` generation so the existing build-derived version display compiles under AGP 9.3.0; application behavior is unchanged.
- Layer 6.1 floating interaction refinement implemented: non-control background gestures move the existing 300 × 62 dp window after Android touch slop and clamp it within current window bounds, while Power, Expand, and Close remain tap-only controls. Main moves its existing task to the background only after the overlay service confirms that the window is present; permission denial or a failed add leaves Main visible. Expand and both Close boundaries remain unchanged. Position is in-memory only, and routing, diagnostics, and product-state mapping remain unchanged; emulator/physical interaction and task validation is required.
- Layer 6 final floating controller implemented (its original visual-order description was superseded by Layer 6.2): the former Layer 5 placeholder surface was replaced by the approved compact 300 × 62 dp graphite controller; the current order is STATUS → POWER → EXPAND → CLOSE, with no separator. It reads the service-owned four-state projection, delegates Power to the established controller operations, reuses Main's task on Expand, and makes floating Close overlay-only. Routing, diagnostics, and product-state mapping remain unchanged; emulator/physical visual and behavior validation is required.
- Layer 5 overlay foundation implemented: Main's Floating control now uses the standard package overlay-permission flow and rechecks the real grant on resume. A separate non-exported, fail-closed overlay service owns one temporary `TYPE_APPLICATION_OVERLAY` test surface across Main backgrounding/recreation and supports overlay-only Close. Main full Close additionally removes the overlay before its established safe controller shutdown/task removal. No overlay-open preference, routing API, playback inspection, diagnostic ownership, or product-state mapping was added; device/emulator validation remains required.
- Layer 4.1 localization foundation implemented: complete English product UI copy is centralized in the default `values/strings.xml`, the unqualified locale is declared as `en-US`, and Android Gradle Plugin locale-config generation enables the standard per-app language resource architecture. The per-app selection work above now uses that foundation; no custom localization framework or diagnostic report changes were added.
- Layer 4 Settings implemented as a compact graphite modal over the unchanged Main screen. The supplied reference is now reflected in the lower-centered 88%-width placement, stronger restrained dim, compact rows, section dividers, copy glyph, fine border, and typography. Settings root, minimal in-sheet Language/Advanced/About child pages, outside/Back dismissal, build-derived version display, and clipboard feedback are present. Copy diagnostic report delegates to the existing service report API and formatter. The protected routing/controller and Layer 2 projection are unchanged; physical visual and interaction validation remains required.
- Layer 3 Main product UI implemented: the normal activity now renders one fixed-geometry OLED-black Compose screen directly from the service-owned `READY` / `WAITING` / `ACTIVE` / `ERROR` projection. Power delegates to the existing controller ON/OFF operations and Close performs existing safe shutdown before removing the task. Four production-layout previews and focused UI/source-contract tests were added. The Settings control is now activated only by Layer 4; the closed Main composition remains unchanged.
- The combined Layer 1.6 + Layer 2 + Layer 3 flow passed the supplied physical run on Xiaomi `2201117TY`: the first ChatGPT Voice session was audibly routed through the telephone earpiece, a second session after ending/restarting Voice routed through the earpiece without toggling Private Audio, Disable restored normal behavior, Close restored normal behavior, and reopening/re-enabling worked again. No diagnostic report was copied for this run.
- Layer 2 product-state projection implemented: `PrivateAudioService` now exposes one Compose-observable, read-only `privateAudioState` with exactly `READY`, `WAITING`, `ACTIVE`, and `ERROR`. OFF has highest precedence; current protected failure precedes current confirmed participating earpiece routing; all other enabled conditions are normal waiting. The pure mapping ignores historical success/reversion and adds no routing behavior. Physical/UI consumption remains unverified until a later layer.
- Layer 1.6 permanent controller implemented: Power ON registers provider-independent public playback observation and waits without audio influence; qualifying communication starts one protected POC-5 cycle; stable inferred external-playback end cleans it fully and returns to waiting under the same enabled intent. Cycle generations protect OFF, destruction, and later sessions from stale timers. Layer 1.5 physically failed because its mode-departure completion boundary remained sticky behind the local track. Layer 1.6 physical validation is required.

- Layer 1.5 multi-session lifecycle implemented: `PrivateAudioService` now owns a non-persisted enabled intent distinct from each one-shot observer experiment. Normal completed-run cleanup reaches `CLEARED` before exactly one fresh waiting arm; Disable clears intent before cleanup, while blocked/telephony failures, shutdown, and process death do not auto-rearm. Protected POC-5 routing, one-request, observation, and cleanup semantics remain unchanged. Physical multi-session validation is still required.
- Layer 1 lifecycle ownership migration completed: one local non-exported started/bindable `PrivateAudioService` now solely owns the existing observer, diagnostic evidence, and POC-5 experiment. Explicit visible Arm promotes it to a `specialUse` foreground service before arming; Disarm/Clear cleans up and ends foreground/started lifetime; activity recreation only rebinds; restart remains fail-closed with `START_NOT_STICKY` and no persistence or automatic re-arm. The diagnostic UI and protected POC-5 routing semantics remain in place.
- POC-5 Layer 0 characterization protection added before lifecycle migration: JVM source-contract tests now lock down trigger gating, telephony/system-priority blocking, silent-track and mode/request ordering, the single-request cap, reentrant/delayed no-retry behavior, failure and session-exit cleanup paths, cleanup ordering/idempotence, and the existing report-copy formatter path without changing production routing behavior.
- Project concept established.
- Primary use case defined.
- Preliminary research completed.
- Android communication-device APIs identified conceptually.
- Safety boundaries established.
- Repository foundation established.
- Repository-first documentation ownership and evidence-driven workflow established.
- Stable AI-session onboarding context added.
- Android application bootstrap completed with Kotlin, Compose, Material 3, and a reproducible Gradle build.
- Observe-only audio diagnostic implemented using public Android APIs, with current-state snapshots, device callbacks, an in-memory event log, and plain-text clipboard report copying.
- Observe-only audio diagnostic compile failure fixed by removing the unavailable `TYPE_ECHO_REFERENCE` device-type label.
- A physical-device observation (device/build metadata not yet recorded) found that ChatGPT Voice start was temporally correlated with `MODE_NORMAL` changing to `MODE_IN_COMMUNICATION`, the communication device changing from the built-in earpiece to the built-in speaker, and speakerphone changing from off to on; ending Voice correlated with the reverse changes. This is an observation, not universal behavior or proof of causation.
- Safe one-shot earpiece-routing PoC implemented: an explicitly armed run can react once to the observed communication state, request the built-in earpiece with the public API, retain diagnostic evidence, and clear its request on session exit, user action, priority modes, or clean activity destruction.
- POC-1 executed on device product `2201117TY`: `setCommunicationDevice(Built-in earpiece)` returned `true`, but Android continued to report the built-in speaker during ChatGPT Voice; after the session Android returned to the earpiece and cleanup completed. Audible routing and software-version metadata were not recorded.
- POC-2 communication-mode participation implemented as a bounded experiment: after explicit arming and the qualifying external state, Private Audio records pre-change state, requests `MODE_IN_COMMUNICATION`, makes one earpiece request, and relinquishes both route and mode participation during cleanup.
- POC-2 executed on device product `2201117TY`: Private Audio successfully requested `MODE_IN_COMMUNICATION` and its single earpiece request returned `true`, but Android continued to report the built-in speaker during ChatGPT Voice; Android returned to the earpiece only after Voice ended.
- POC-3 bounded route reassertion implemented: an armed run makes no more than three earpiece requests, spaces additional attempts by 750 ms, revalidates eligibility before each attempt, records per-attempt evidence and route outcomes, and cancels delayed work during cleanup.
- POC-3 executed: all three bounded earpiece requests returned `true`, but Android never reported the built-in earpiece while ChatGPT Voice remained active.
- POC-4 explicit mode-ownership experiment implemented: an armed run records the qualifying state, briefly transitions through and verifies `MODE_NORMAL`, immediately re-establishes and verifies `MODE_IN_COMMUNICATION`, makes exactly one earpiece request, and records immediate, callback, and one-second observations.
- POC-4 diagnostics enriched without changing its experiment sequence: the copied report now correlates Private Audio PID/UID/package and process importance with four compact state snapshots, including public active-playback usage/content/capture-policy/device fields, plus an explicit external `dumpsys audio` correlation section. An immediate corrective iteration removed unavailable playback-state and session-ID members and treats `getActivePlaybackConfigurations()` as the active snapshot.
- POC-4 executed on the tested Xiaomi device: its explicit communication-mode transition did not physically move active ChatGPT Voice from the built-in speaker to the earpiece.
- POC-5 active-requester experiment implemented in place of the unexecuted POC-4 sequence: after explicit arming and the qualifying external state, Private Audio starts a mono PCM silent `USAGE_VOICE_COMMUNICATION`/`CONTENT_TYPE_SPEECH` `AudioTrack`, confirms `PLAYSTATE_PLAYING`, records visible active playback, requests `MODE_IN_COMMUNICATION`, and makes exactly one earpiece request. Cleanup stops the writer, clears routing and mode participation, and stops, flushes, and releases the track.
- POC-5 mode-request path audited after physical evidence contained no successful Private Audio `setMode` AudioService event. The implementation already explicitly called `setMode(MODE_IN_COMMUNICATION)` after confirming the silent track was playing and before its single earpiece request; diagnostics now record the invocation timestamp and thread, local track state, modes immediately before/after, exact exception if any, and ordering of the route request. This application-side evidence does not establish actual mode ownership.
- A physical POC-5 run on Xiaomi `2201117TY` (Android 13/API 33) confirmed the instrumented application-side sequence and active Private Audio communication playback. A simultaneous live audio dump showed Private Audio's earpiece route client but no Private Audio mode requester; ChatGPT remained the sole active mode owner and its speaker route won. Repository and Android 13 framework audit found that the source manifest omits the documented `MODIFY_AUDIO_SETTINGS` permission and that AOSP silently returns from `setMode()` when this permission check fails, but the observed accepted route client prevents treating the omission as the proven cause until the installed package permission state and focused AudioService log are captured.
- Follow-up physical diagnostics confirmed that the installed Private Audio APK did not request `android.permission.MODIFY_AUDIO_SETTINGS`; focused system logcat captured AudioService receiving Private Audio's `setMode(MODE_IN_COMMUNICATION)` call and rejecting it with `Audio Settings Permission Denial: setMode()`. The manifest now requests the required public permission without changing POC-5 routing behavior.
- POC-5 passed on Xiaomi `2201117TY` running Android 13/API 33 with ChatGPT package `com.openai.chatgpt` and Private Audio package `app.privateaudio`. Diagnostics confirmed the silent communication track was created and playing, visible as active `VOICE_COMMUNICATION` playback, active before the explicit mode request, followed by exactly one accepted earpiece request. Android changed from built-in speaker/speakerphone on to built-in earpiece/speakerphone off while ChatGPT Voice remained active, and active communication playback configurations reported built-in earpiece/device ID 2.
- Repeated physical listening after a full uninstall and reinstall confirmed active ChatGPT Voice audio through the phone's upper built-in earpiece. POC-5 is Android-reported PASS, human-audible PASS, and overall PASS—the project's first confirmed physical success. The reinstall preceded the first repeatable audible success, but causality is not established.

## CURRENT

- Added Javanese / Basa Jawa (`jv`) as one complete neutral, FULL-reviewed and frozen LTR product locale in `values-jv`, using Latin script and the default `jv-Latn-ID` identity. The frozen 28 stable strings have PASS HIGH linguistic review with 0 FIX, 0 POLISH, and 0 HOLD; preserve ON `Urip` versus ACTIVE `Aktif`, the complete `Siyap` / `Ngenteni` / `Aktif` / `Kesalahan` Mini paradigm, `AI`, `Mini`, Settings/Default/Advanced terminology, audio switching, exact built-in call-earpiece wording `earpiece gawan telpon`, diagnostic-report terminology, and parallel accessibility actions. `settings_fake_phone_pre_arm` remains intentionally excluded, and no legacy `values-jw`, regional, script, Aksara Jawa, or compact Mini resource tree was added. Persistent Layer 4.1 source contracts cover the qualifier identity, frozen terminology, key/placeholder scope, unsafe-tree absence, and no compact override. Android instrumentation coverage resolves localized resources and formatted controller text with LTR configuration for `jv`, `jv-ID`, and `jv-Latn-ID`, checks generated LocaleConfig discovery of `jv`, and measures the four resolved natural Mini labels through the shared production-equivalent 16f / 15f / 14f selector. Static/source-contract validation is complete; actual instrumentation execution, selected Mini size, runtime locale switching and rendering, emulator behavior, glyph/clipping presentation, and physical-device presentation remain unverified. No routing, audio, proximity, controller, Fake Phone, diagnostic-core, English/default, or existing-locale behavior changed.

- Added Armenian (`hy`) as one complete neutral, FULL-reviewed and targeted-MINI-reviewed LTR product locale in `values-hy`, using Armenian script (`Armn`) and the default `hy-Armn-AM` identity. The frozen 28 stable product strings preserve the unchanged source keys and placeholders with PASS HIGH, 0 FIX, 0 POLISH, and 0 HOLD; `settings_fake_phone_pre_arm` is intentionally excluded and continues to fall back to English. Persistent Layer 4.1 coverage protects Armenian script, ON `Միացված է` versus ACTIVE `Ակտիվ`, the full `Պատրաստ է` / `Սպասում է` / `Ակտիվ` / `Սխալ` state paradigm, `ԱԲ`, `Մինի`, Settings/Default/Advanced terminology, built-in call-earpiece and audio-output wording, diagnostic-report terminology, accessibility action semantics, the neutral qualifier, absence of duplicate regional/BCP-47 trees, and absence of an Armenian Mini compact override. Android instrumentation covers actual Armenian resource resolution, localized Settings and controller accessibility text, and LTR configuration for `hy`, `hy-AM`, and `hy-Armn-AM`; generated LocaleConfig discovery of `hy`; and production-equivalent shared 16f / 15f / 14f measurement of all four natural Mini labels. Western Armenian (`hyw`) is out of scope, no duplicate regional resource tree was added, and no routing, audio, proximity, controller, diagnostic-core, Fake Phone, or other production behavior changed. Linguistic review and static/source-contract checks are complete. Gradle JVM tests could not run because the unavailable Gradle 9.5.0 distribution was blocked by the environment’s HTTP 403 proxy; Android instrumentation was not executed because no emulator or device was available. Therefore actual runtime resource resolution and LocaleConfig behavior, the selected common Mini size, Armenian glyph/shaping and runtime rendering, emulator behavior, and physical-device presentation remain unverified.

- Added the default-OFF experimental Fake Phone pre-arm Advanced control. Eligible `WAITING` may prepare one protected silent communication context and earpiece request; pre-arm alone remains `WAITING`, real qualifying playback promotes the same resources without duplicate creation/request, and optional failure or safety/lifecycle exits clean experiment ownership without disabling normal fallback routing. The experiment does not mute or change global volume, its label intentionally exists only in English/base resources, public playback metadata does not establish owner or audible routing, and Xiaomi physical confirmation remains required. Toggle OFF preserves the established clean waiting and POC-5 behavior.

- Added Nepali (`ne`) as one complete neutral, FULL-reviewed and targeted-mini-reviewed LTR product locale in generic `values-ne`, using Devanagari and the default `ne-Deva-NP` identity. The unchanged 28-key English source contract and placeholders are complete, and persistent Layer 4.1 coverage freezes `एआई`, ON `अन छ` versus ACTIVE `सक्रिय`, the full `तयार` / `पर्खँदै` / `सक्रिय` / `त्रुटि` state paradigm, `डिफल्ट`, `उन्नत`, reviewed `मिनी`, audio-output switching wording, the built-in `ह्यान्डसेट इयरपिस`, and the parallel controller description. Android instrumentation now covers actual resource resolution and LTR configuration for `ne`, `ne-NP`, and `ne-Deva-NP`, generated LocaleConfig discovery of `ne`, and production-equivalent shared 16f / 15f / 14f measurement of all four natural Mini labels with one common selected size and no Nepali compact override. Static XML, source-contract, frozen-literal, key/placeholder parity, qualifier-tree, conflict-marker, and whitespace validation completed successfully. Gradle JVM tests could not run because the unavailable Gradle 9.5.0 distribution was blocked by the environment’s HTTP 403 proxy, and Android instrumentation was not executed because no emulator or device was available; therefore runtime locale switching and resource resolution, rendered glyph/shaping presentation, the actually selected common Mini size, emulator behavior, and physical-device presentation remain unverified.

- Refined the repository-local Private Audio localization skill into the reusable execution contract for short ADD LOCALE tasks. A task can now supply only frozen locale-specific identity, qualifier, script/direction, intended tags, exact resources, reviewed terminology/exceptions, and unresolved warnings while automatically invoking the current source, qualifier, parity, semantic, resource-resolution, Mini measurement, accessibility, regression, documentation, validation, and evidence-reporting gates. The authoritative localization policy, translations, Android resources, production code, and tests are unchanged.

- Added Somali (`so`) as one complete neutral, FULL-reviewed and targeted-Mini-reviewed LTR product locale in generic `values-so`, using Latin script and the default `so-Latn-SO` identity. The unchanged 28-key English source contract and placeholders are complete, and persistent Layer 4.1 coverage freezes `AI`, ON `daaran` versus ACTIVE `Firfircoon`, the full `Diyaar` / `Sugaya` / `Firfircoon` / `Khalad` state paradigm, Default `Caadi`, Advanced `Dejinta horumarsan`, Mini `Mini`, routing `Sugaya in la beddelo halka codku ka soo baxayo`, and the built-in call earpiece `sameecadda sare ee taleefanka` (review confidence MEDIUM-HIGH) without headphone wording. Android instrumentation now covers generic resource resolution and LTR configuration for `so`, `so-SO`, `so-DJ`, `so-ET`, and `so-KE`, generated LocaleConfig discovery of `so`, and production-equivalent shared 16f / 15f / 14f measurement of all four natural Mini labels with one common selected size and no Somali compact override. Static XML, source-contract, frozen-literal, placeholder, qualifier-tree, conflict-marker, and whitespace validation were executed successfully. Gradle JVM tests could not execute because the unavailable Gradle 9.5.0 distribution was blocked by the environment's HTTP 403 proxy, and Android instrumentation was not executed because no emulator or device was available; therefore the selected common Mini size, runtime locale switching, rendered presentation, emulator behavior, and physical-device behavior remain unverified. Routing, diagnostic-core, proximity, audio, Mini geometry/measured-fit, shared direction/layout architecture, English/default copy, and existing locale copy are unchanged.

- Added Malayalam (`ml`) as a complete, linguistically reviewed and frozen LTR product locale in the standard `values-ml` Android resource qualifier. Its Malayalam-script resources preserve the unchanged 28-key English source contract and placeholders, and use the shared full-state Mini aliases without a Malayalam-specific compact override, runtime branch, or geometry change. Persistent Layer 4.1 coverage protects the standard logical tag and qualifier, Malayalam Unicode script, reviewed `AI` and `മിനി` choices, ON (`ഓണാണ്`) versus ACTIVE (`സജീവം`), built-in earpiece and Advanced terminology, and the full Ready / Waiting / Active / Error paradigm. Android instrumentation coverage now resolves and measures all four Malayalam Mini labels through the production-equivalent shared `TextPaint` fitting path and verifies LTR resource configuration; the selected common size, runtime locale switching, glyph shaping/rendering, emulator presentation, and physical-device presentation remain unverified until instrumentation is actually executed.

- Implemented one language-independent measured Mini status font-fit rule before further locale expansion. Mini resolves the current locale's complete Ready / Waiting / Active / Error visual paradigm and uses actual `sans-serif`/normal rendered widths to choose the largest one shared size from 16f, 15f, and 14f that keeps every label within the established 96-unit non-ellipsis target inside the unchanged 100-unit slot. Fourteen remains the hard automatic minimum and existing single-line ellipsis remains final safety. The choice is calculated when the overlay view is created and recalculated on localized configuration refresh, never on state transitions. Deterministic JVM coverage protects all three candidates, the hard minimum, all-state/common-size selection, measured-width rather than character-count behavior, refresh/state boundaries, unchanged aliases/accessibility/geometry, and frozen Tamil/Gujarati overrides; Android instrumentation coverage exercises resolved locale resources with production `TextPaint` assumptions. Source/contract validation is complete; instrumentation, emulator/runtime rendering, and physical-device validation remain unexecuted and unverified. Routing, diagnostics, proximity behavior, translations, shared direction-driven RTL architecture, 300 × 62 geometry, 100-unit slot, and control positions are unchanged.

- Added Hebrew (`he`) and Yiddish (`yi`) as complete frozen RTL product locales, using Android's legacy-compatible `values-iw` and `values-ji` resource qualifiers so API 31–34 resource matching remains supported while Java/Android locale canonicalization and generated locale discovery expose the modern logical tags `he` and `yi`. Both locales have the unchanged English source contract's 28 keys and placeholders. Hebrew completed FULL linguistic review and the mandatory MINI adversarial review. Yiddish completed FULL and mandatory MINI reviews, contemporary Hasidic-usage and broad-readability reviews, and a dedicated built-in call-earpiece terminology review. Independent persistent Layer 4.1 contracts protect each locale's modern identity and legacy qualifier, native name, frozen AI choice, ON-versus-ACTIVE distinction, audio-output routing, device/system-language and screen-proximity semantics, reviewed built-in call-earpiece phrase, parallel accessibility actions, and all four Mini states; Yiddish additionally rejects the abandoned `איי־אײַ` AI form. Source-contract, XML, static RTL/bidi, and generated-discovery validation are complete without manual direction marks or production UI changes. Android runtime locale switching, glyph/rendering, and Main/Settings/Mini visual presentation remain unverified.

- Added Serbian Latin for Montenegro (`sr-Latn-ME`) and Maltese (`mt`) as complete, independently reviewed and frozen LTR product locales in standard `values-b+sr+Latn+ME` and `values-mt` Android resources. Each locale's 28 keys and format placeholders align with the unchanged English fallback, and Android's generated locale configuration discovers both by their native/display locale names without a parallel registry. Independent persistent Layer 4.1 contracts protect each locale's reviewed AI terminology, ON-versus-ACTIVE distinction, audio-output routing, device/system-language and screen-proximity semantics, built-in call-earpiece wording, parallel accessibility actions, and all four Mini states. The Montenegro contract additionally protects its Latin script, Montenegro region, ijekavian vocabulary, and separation from unchanged ordinary Serbian Latin and Serbian Cyrillic resources; the Maltese contract protects the reviewed `Issettjar` terminology and descriptive upper call-receiver wording. This additive localization batch is linguistically reviewed and source-contract tested; Android runtime locale switching, rendering, and Mini presentation remain unverified.

- Added Greek (`el`) as a complete, independently FULL-reviewed and frozen LTR product locale in standard `values-el` Android resources. Its 28 keys and format placeholders align with the unchanged English fallback, and Android's generated locale configuration discovers it by the native/display name `Ελληνικά`. An independent persistent Layer 4.1 contract protects the reviewed `AI` terminology, ON-versus-ACTIVE distinction, audio-output switching, device/system-language and screen-proximity semantics, built-in call-earpiece terminology, parallel accessibility actions, and all four Mini states. This additive localization change is linguistically reviewed and source-contract tested; Android runtime locale switching, rendering, and Mini presentation remain unverified.

- Added Bulgarian (`bg`), default Cyrillic Serbian (`sr`), and Macedonian (`mk`) as complete, independently FULL-reviewed and frozen LTR product locales in standard Android resources. Each locale's 28 keys and format placeholders align with the unchanged English fallback, and Android's generated locale configuration discovers all three by their native/display locale names. Independent persistent Layer 4.1 contracts protect each locale's Cyrillic script, reviewed AI terminology, ON-versus-ACTIVE distinction, audio-switching, device/system-language and screen-proximity semantics, built-in call-earpiece terminology, parallel accessibility actions, and all four Mini states. The contracts additionally keep default Cyrillic Serbian separate from the existing Latin-script `sr-Latn` resources and terminology. This additive localization batch is linguistically reviewed and source-contract tested; Android runtime locale switching, rendering, and Mini presentation remain unverified.

- Added Slovenian (`sl`), Croatian (`hr`), Serbian Latin (`sr-Latn`), and Bosnian (`bs`) as complete, independently FULL-reviewed and frozen LTR product locales in standard Android resources, using the BCP-47 `values-b+sr+Latn` qualifier for Latin-script Serbian. Each locale’s 28 keys and format placeholders align with the unchanged English fallback, and Android’s generated locale configuration discovers all four by their native/display locale names without a parallel registry or locale-specific application logic. Separate persistent locale-specific Layer 4.1 contracts protect each locale’s ON-versus-ACTIVE distinction, audio-switching, device/system-language and screen-proximity semantics, built-in call-earpiece terminology, parallel accessibility actions, reviewed AI terminology, all four Mini states, and intentionally distinct regional vocabulary; Serbian is additionally protected as Latin script. This additive localization batch is linguistically reviewed and source-contract tested; Android runtime locale switching, rendering, and Mini presentation remain unverified.

- Added complete Dutch (`nl`), Afrikaans (`af`), and Luxembourgish (`lb`) Android locale resources from independently completed FULL linguistic reviews. Layer 4.1 contracts preserve each locale’s native name, ON-versus-ACTIVE distinction, audio-switching, device/system-language and screen-proximity semantics, built-in call-earpiece terminology, parallel accessibility actions, and all four reviewed Mini states. This localization-only batch is source-contract tested; runtime, emulator, and physical-device presentation remain unverified.

- Fixed the Settings diagnostic-save lifecycle failure: Main now freezes the service report before launching Android's document picker, then writes that immutable UTF-8 text after the activity returns without requiring a current service binding. Cancellation and every completion path clear the pending report; null document Uris, null output streams, absent service at tap time, and write exception details are retained in local diagnostics where available. Activity/service binding, routing, POC-5, triggers, proximity, and controller behavior are unchanged. Physical save-picker validation remains pending.

- Settings now saves the on-demand UTF-8 plain-text diagnostic through Android’s Storage Access Framework document picker instead of the clipboard, with localized save/error feedback and no storage permission. Diagnostic format 2 adds public build/device environment metadata, explicit bounded-buffer retention/drop counts, and a redundant-playback-callback suppression count. Playback callbacks still reach routing/session detection unchanged; only repeated diagnostic serialization is suppressed, while every public playback metadata change remains recorded. Physical save-picker and routing smoke validation remain pending.

- Completed routing-cycle diagnostics now retain the most recent meaningful participated experiment immediately after its final post-cleanup observation and before the permanent controller replaces the live experiment with a fresh `ARMED` cycle. The copied report keeps the existing current-experiment section unchanged and adds a separate last-completed summary with trigger origin, request/attempt outcome, POC-5 snapshots, cleanup result/reason/time, and final mode. Fresh unused `IDLE`/`ARMED` states are not recorded. Routing, triggers, cleanup ordering/timing, re-arm behavior, proximity, and product state are unchanged.
- A diagnostics-only startup audio trace is implemented in the existing copied report. While Power is ON, the existing public `AudioPlaybackCallback` now preserves bounded, millisecond-timestamped playback appearance/disappearance snapshots, exact usage/content/flags, allowed-capture policy in playback reports, output-device metadata, and adjacent mode/communication-device/speakerphone state. Configuration presence in the callback's active snapshot drives appearance/disappearance; the unavailable per-configuration `AudioPlaybackConfiguration.isActive` claim has been removed. Existing controller events share the trace. It does not classify the startup sound, add polling or permissions, or change routing; Xiaomi physical characterization remains pending.
- **Layer 7 implementation is DONE after the Layer 7C audit. Core Xiaomi near/off and far/on behavior is physically confirmed; the remaining individually unrecorded Layer 7 physical matrix cases stay NOT TESTED / UNKNOWN.**

## NEXT

- Physically evaluate the state-motion cadence and feel: WAITING breathing, WAITING → ACTIVE transition, ACTIVE dot prominence, Main/Mini consistency, and calmness versus distraction. Automated tests cannot establish final visual approval.
- Finish the outstanding current UI and lifecycle validation in `TEST_PLAN.md`, especially floating permission, drag/bounds, task reuse, overlay recreation, both Close boundaries, and process-death behavior; complete unrelated Settings checks still lacking runtime evidence.
- Execute the remaining optional Layer 7 physical regression/characterization matrix in `TEST_PLAN.md`, including setting persistence and lifecycle/accessory/telephony boundaries, without treating it as already observed.
- Run final regression and V1-readiness validation, including the remaining physical routing, cleanup, multi-session, and real-call safety checks.

## UNKNOWN

- Which public playback configuration, if any, corresponds to the audible ChatGPT Voice startup sound on the Xiaomi test device; its usage/content type, overlap with main voice playback, and route relative to Private Audio's mode/device requests require the pending timestamp-correlated physical run.
- Why full uninstall and reinstall preceded the first repeatable physically audible success; no causal explanation is established.
- Whether the confirmed POC-5 result remains stable across untested lifecycle transitions, APK updates, other devices, OEMs, and software versions.
- Whether all Chromium browsers expose the Chrome-tested metadata and whether Gecko/Firefox exposes a supported signature.
- OEM-specific routing differences.
- Exact behavior during incoming and outgoing real phone calls.
- Whether communication-device callbacks are delivered while this application is backgrounded on tested Android/OEM builds, and whether process lifetime interrupts an experiment.
